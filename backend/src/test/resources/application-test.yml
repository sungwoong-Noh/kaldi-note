spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    org.hibernate.SQL: warn

kaldi:
  jwt:
    secret: local-development-only-secret-key-32bytes-minimum
  oauth:
    kakao:
      client-id: dummy
    google:
      client-id: dummy
      client-secret: dummy
  oci:
    tenancy-id: dummy
    user-id: dummy
    fingerprint: dummy
    private-key: dummy
    region: ap-chuncheon-1
    namespace: dummy
    bucket-name: dummy
