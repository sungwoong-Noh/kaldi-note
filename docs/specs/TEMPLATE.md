---
id: <대문자 약어. AC ID의 접두어가 된다. 예: GRIND, RECIPE, BREWLOG>
title: <기능 이름>
status: 초안
milestone: <docs/ROADMAP.md의 마일스톤(M1 등). 상시 트랙이면 비워둔다>
supersedes: <이 스펙이 대체하는 이전 스펙 경로. 없으면 비워둔다>
---

# <기능 이름> 스펙

> 작성 규칙은 [`docs/specs/README.md`](README.md) 참조.
> **모든 인수 조건은 자동화된 테스트로 옮길 수 있어야 한다.**

## 시나리오

<!--
이 기능이 하는 일을 2~4문장으로. 명사가 아니라 동작으로 쓴다.
"레시피 관리 기능" (X) → "사용자가 푸어 스텝 시퀀스를 포함한 레시피를 만들고 수정한다" (O)
누가 언제 쓰는가도 포함한다.
-->

### 범위 밖 (Non-goals)

<!-- 이번에 만들지 않는 것. 없으면 구현자가 범위를 늘린다. -->

- 

## 용어

<!-- 이 스펙에서 쓰는 도메인 용어. 애매하면 여기서 못박는다. 없으면 이 절을 지운다. -->

| 용어 | 정의 |
|---|---|
| | |

## 데이터

<!-- 새로 만들거나 바꾸는 테이블·필드. 타입과 nullable 여부를 명시한다. 없으면 절을 지운다. -->

| 테이블 | 컬럼 | 타입 | Null | 설명 |
|---|---|---|---|---|
| | | | | |

## 계약

<!--
API가 있으면 docs/contracts/의 OpenAPI 조각을 가리킨다. 없으면 이 절을 지운다.
계약 도구가 아직 없는 기능이면(session ② 전) 요청/응답 예시로 대신한다.
-->

| 메서드 | 경로 | 인증 | 계약 파일 |
|---|---|---|---|
| | | | |

```json
```

---

## 인수 조건

> 각 조건은 리터럴 값을 쓴다. "적절히", "빠르게", "잘 동작한다" 같은 표현은 금지다.
> 네 갈래(기능·엣지·실패·비기능)를 전부 살핀다 — 관련 없는 갈래는 절 자체를 지운다.
> ID는 한 번 부여하면 바꾸지 않는다. 테스트가 이 ID로 스펙을 역참조한다.

### 기능

#### AC-<ID>-01 · <한 줄 요약>

- **Given** <전제 상태. 구체적인 값으로>
- **When** <행위>
- **Then** <결과. 정확한 값으로>
- **검증** <단위 테스트 `XxxTest` / 통합 테스트 `XxxTest` / API 테스트 `XxxControllerTest` / e2e>

### 엣지 (경계값)

<!-- "18~22%가 이상 구간"으로 끝내지 말고, 18.0이 포함인지 17.9가 제외인지 각각 쓴다. -->

#### AC-<ID>-10 · <경계값 포함>

- **Given** 
- **When** 
- **Then** 
- **검증** 

### 실패

<!-- HTTP 상태와 code를 반드시 명시한다. -->

#### AC-<ID>-20 · <에러 상황>

- **Given** 
- **When** 
- **Then** HTTP `<상태코드>`와 `code: "<ERROR_CODE>"`를 반환한다
- **검증** 

### 비기능

<!-- 성능·용량 제약이 실제로 있을 때만. 없으면 절을 지운다. -->

#### AC-<ID>-30 · <제약>

- **Given** 
- **When** 
- **Then** 
- **검증** 

---

## 구현 순서

<!--
태스크 단위. 예전에 별도 계획 문서가 하던 역할을 흡수한 절이다(`docs/archive/plans/`).
각 태스크는 TDD 사이클 하나(실패 테스트 → 실패 확인 → 최소 구현 → 통과 확인 → 커밋)로 끝난다.
스펙의 모든 AC가 어느 태스크에 매핑되는지 명시한다 — 빠진 AC가 있으면 이 절이 미완성이다.
-->

- [ ] **Task 1: <이름>** — Covers: AC-<ID>-01, AC-<ID>-10

  ```<lang>
  <실제로 붙여넣을 수 있는 코드 스케치>
  ```

---

## 수동 확인

<!--
자동화할 수 없는 것만. 차단형(그 확인 없이는 "동작한다"고 말할 수 없는 것)은 ★를 붙인다.
차단형이 하나라도 남아 있으면 status를 `구현완료`로 올리지 않는다. 없으면 "없음"이라고 쓴다.
-->

- [ ] 

## 열어둔 결정

<!-- 지금 정하지 않고 미루는 것. 언제 정할지도 함께 적는다. 없으면 "없음". -->

- 
